# Vendored drop-in packages: committed so Edge Delivery Services can serve
# them without a build step, but not hand-authored or meant to be reviewed
# line-by-line. GitHub collapses these in PR diffs and excludes them from
# language stats.
scripts/__dropins__/** linguist-generated=true linguist-vendored=true

# Regenerated by `npm install` / `npm ci`, not hand-edited.
package-lock.json linguist-generated=true
cypress/package-lock.json linguist-generated=true
